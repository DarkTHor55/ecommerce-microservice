spring:
  datasource:
    url: jdbc:mysql://localhost:3306/payment
    username: root
    password: Rohit@123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  kafka:
    producer:
      bootstrap-servers: localhost:9093
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframewotk.kafka.support.serializer.jsonSerializer
      properties:
        spring.jason.type.mapping: paymentConfirmation:com.darkthor.payment_server.Request.PaymentNotificationRequest
server:
  port: 8040


